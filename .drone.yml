kind: pipeline
name: test

steps:
- name: build
  image: golang
  commands:
    - echo -n "Hello"

steps:
- name: docker  
  image: plugins/docker
  settings:
    repo: arenstar/puppet-knock
    tags: latest
